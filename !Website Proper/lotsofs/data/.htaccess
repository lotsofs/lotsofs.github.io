# Holds the SQLite database. Must never be served.
# Already outside the web root; this is defense in depth.
<IfModule mod_authz_core.c>
	Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
	Order allow,deny
	Deny from all
</IfModule>
